name: Deploy Dashboard (manual)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Build Dashboard (placeholder)
        run: |
          echo "Add your dashboard deploy steps here."
