*** Begin Patch
*** Update File: satpambot/bot/modules/discord_bot/cogs/a24_autolearn_qna_autoreply.py
@@
-        answer = await self._ask_llm(question)
-        if not answer:
-            self.log.warning("[autolearn] no answer")
-            return
+        answer = await self._ask_llm(question)
+        if not answer:
+            try:
+                answer = await self._ask_llm(question, prefer="gemini")
+            except Exception:
+                answer = None
+        if not answer:
+            self.log.warning("[autolearn] no answer")
+            return
@@
-        await channel.send(content=f"[auto-learn]\nQ: {question}")
+        from discord import Embed
+        provider = getattr(self, "_last_provider", "LLM")
+        emb = Embed(title="[auto-learn] QnA")
+        emb.add_field(name="Question (Leina)", value=(question or "—")[:1024], inline=False)
+        emb.add_field(name=f"Answer ({provider})", value=(answer or "—")[:1024], inline=False)
+        await channel.send(embed=emb)
*** End Patch
