*** Begin Patch
*** Add File: satpambot/bot/modules/discord_bot/cogs/a06_embed_scribe_update_fallback_async_overlay.py
+import asyncio
+
+async def _noop(*args, **kwargs):
+    return None
+
+def _ensure_coroutine(fn):
+    if asyncio.iscoroutinefunction(fn):
+        return fn
+    if callable(fn):
+        async def wrapper(*a, **kw):
+            return fn(*a, **kw)
+        return wrapper
+    return _noop
+
+async def setup(bot):
+    try:
+        from satpambot.bot.utils.embed_scribe import EmbedScribe
+    except Exception:
+        return
+    EmbedScribe.post = _ensure_coroutine(getattr(EmbedScribe, "post", _noop))
+    EmbedScribe.update = _ensure_coroutine(getattr(EmbedScribe, "update", _noop))
*** End Patch
