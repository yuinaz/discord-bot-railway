This is a drop-in translation patch for SatpamBot.

Install:
  python -m pip install -r requirements.latest.txt

Smoketest:
  python scripts/smoke_translator.py

Enable cog (if needed):
  !load satpambot.bot.modules.discord_bot.cogs.translator
