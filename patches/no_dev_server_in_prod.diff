*** a/satpambot/dashboard/app_dashboard.py
--- b/satpambot/dashboard/app_dashboard.py
@@
-        app.run(host="0.0.0.0", port=_satp_port, debug=False)
+        # Hanya jalankan Flask dev server saat LOCAL DEV.
+        import os
+        if os.getenv("RUN_LOCAL_DEV", "0") == "1" and __name__ == "__main__":
+            app.run(host="0.0.0.0", port=_satp_port, debug=False)

*** a/satpambot/helpers/web_safe_start.py
--- b/satpambot/helpers/web_safe_start.py
@@
-            app.run(host=host, port=port, use_reloader=False)
+            import os, logging
+            # Di production (Render), web server utama dijalankan oleh entrypoint lain,
+            # jadi jangan start dev server Flask di sini.
+            if os.getenv("RUN_LOCAL_DEV", "0") == "1":
+                app.run(host=host, port=port, use_reloader=False)
+            else:
+                logging.getLogger(__name__).info(
+                    "[web-safe-start] RUN_LOCAL_DEV!=1 -> skip app.run(); prod web server handled by main.py."
+                )
