--- a/satpambot/bot/modules/discord_bot/cogs/public_chat_gate.py
    +++ b/satpambot/bot/modules/discord_bot/cogs/public_chat_gate.py
    @@
    -bot.add_cog(PublicChatGate(bot)) if hasattr(bot, "add_cog") else PublicChatGate(bot)
    +import inspect
async def maybe_await(x):
    if inspect.isawaitable(x):
        return await x
    return x
async def setup(bot):
    try:
        await maybe_await(bot.add_cog(PublicChatGate(bot)))
    except Exception:
        # final fallback to sync register without crashing boot
        try:
            bot.add_cog(PublicChatGate(bot))
        except Exception:
            pass
