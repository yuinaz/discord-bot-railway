--- a/satpambot/bot/modules/discord_bot/cogs/a06_sticky_status_strict_overlay.py
    +++ b/satpambot/bot/modules/discord_bot/cogs/a06_sticky_status_strict_overlay.py
    @@
    -bot.add_cog(_StickyStatusStrict(bot))
    +import inspect
async def maybe_await(x):
    if inspect.isawaitable(x):
        return await x
    return x
async def setup(bot):
    await maybe_await(bot.add_cog(_StickyStatusStrict(bot)))
