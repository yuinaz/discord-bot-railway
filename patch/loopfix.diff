--- a/satpambot/bot/modules/discord_bot/cogs/repo_guild_sync_bootstrap.py
+++ b/satpambot/bot/modules/discord_bot/cogs/repo_guild_sync_bootstrap.py
@@
+import asyncio
 from discord.ext import commands
 ...
 class RepoGuildSyncBootstrap(commands.Cog):
     def __init__(self, bot):
         self.bot = bot
-        self._task = bot.loop.create_task(self._bootstrap())
+        # Use the running loop instead of bot.loop to avoid DummyBot AttributeError during smoketests
+        self._task = asyncio.create_task(self._bootstrap())
